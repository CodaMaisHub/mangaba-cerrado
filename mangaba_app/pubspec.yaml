name: mangaba_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0+4  # Ensure this line existsflutter pub get
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.4
  geolocator: ^11.0.0
  permission_handler: ^10.2.0
  google_maps_flutter: ^2.6.0
  flutter_map: ^6.2.1
  latlong2: ^0.9.0
  google_maps_flutter_web: ^0.5.12
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
